# 싱큐(SynQ) 백엔드 작업 정리 및 개선 로드맵

> 합의보다, 더 나은 선택.

- 문서 목적: 지금까지 진행한 백엔드 작업을 정리하고, 현재 한계와 후속 개선 과제를 공유한다.
- 대상 프로젝트: 싱큐(SynQ)
- 담당 범위: Build-B — FastAPI 백엔드 및 서비스 통합
- 현재 기준: 2026-08-20, `main` 브랜치
- 현재 검증 상태: 백엔드 자동화 테스트 `85 passed`

---

## 1. 백엔드가 해결하는 문제

싱큐의 백엔드는 단순히 투표 결과를 저장하는 서버가 아니다. 팀원이 각 선택지를 여러 평가 기준으로 점수화하고, 기준별 중요도를 제출하면 다음 과정을 일관된 API 흐름으로 제공한다.

1. 의사결정 방을 생성한다.
2. 익명 또는 이름 제출 방식으로 팀원의 의견을 수집한다.
3. 예정 인원이 모두 제출될 때까지 중간 결과 공개를 제한한다.
4. 입력된 수치만으로 합의도, 안정성, Flip Point와 숨은 갈등을 계산한다.
5. 계산 결과를 근거로 Devil's Advocate 질문과 후속 공방을 제공한다.
6. Google 로그인, 저장소, 선택적 LLM, 프론트엔드와의 연결을 하나의 배포 가능한 서비스로 통합한다.

핵심 설계 원칙은 다음과 같다.

- 수치 계산과 자연어 해석을 분리한다.
- LLM 장애가 핵심 제출과 통계 분석을 중단시키지 않도록 한다.
- 익명 의견과 Google 로그인 계정을 자동으로 연결하지 않는다.
- 입력 오류를 가능한 한 API 경계에서 조기에 차단한다.
- 로컬과 운영 환경의 저장소를 분리하되 같은 API 계약을 유지한다.

---

## 2. 현재 시스템 구조

```text
React SPA
   │
   │ HTTP / Cookie
   ▼
FastAPI
   ├── Google ID Token 검증 및 서명 세션
   ├── 방 생성·조회·제출·분석 API
   ├── Pydantic 입력/응답 검증
   ├── NumPy 통계 엔진
   ├── 선택적 OpenAI 자연어 처리
   └── RoomStore
         ├── 로컬: 인메모리 dict
         └── 운영: DynamoDB

단일 Docker 이미지
   └── React 정적 파일 + FastAPI 런타임
         └── ECR → AWS App Runner
```

### 사용 기술

- API 서버: FastAPI, Uvicorn
- 데이터 검증: Pydantic v2
- 통계 계산: NumPy
- 인증: Google Identity Services, `google-auth`
- 세션: HMAC-SHA256 서명 쿠키
- 저장소: 인메모리 저장소, DynamoDB
- AI 연동: OpenAI API 선택 연동
- 배포: Docker, ECR, AWS App Runner
- 자동화 검증: Pytest, 프론트 빌드, Docker health check, 데모 계약 테스트

---

## 3. 지금까지 완료한 작업

### 3.1 FastAPI 핵심 API 구현 및 통합

방 생성부터 최종 분석까지 프론트엔드가 사용할 API 흐름을 구현했다.

- 방 생성
- 6자리 공유 코드 생성
- 방 정보 및 제출 현황 조회
- 익명·이름 방식 의견 제출
- 전원 제출 여부 확인
- 통계 분석 결과 조회
- Devil's Advocate 공방 상태 조회
- Defender 답변 제출 및 최종 판정
- 서비스 health check

주요 상태 코드를 의미에 맞게 구분했다.

- `201 Created`: 방 생성 또는 의견 제출 성공
- `401 Unauthorized`: 인증이 필요한 이름 제출 방을 비로그인 상태로 생성
- `404 Not Found`: 존재하지 않는 방
- `409 Conflict`: 방 정원 초과, 이름 중복 제출, 전원 제출 전 분석, 이미 종료된 공방
- `422 Unprocessable Entity`: 입력 형식 또는 방의 선택지·평가기준 계약 위반
- `503 Service Unavailable`: Google 인증 설정 누락 또는 Google 검증 서비스 장애

### 3.2 엄격한 입력 모델과 API 계약

Pydantic 모델을 사용해 잘못된 데이터가 통계 및 저장 계층으로 넘어가기 전에 차단했다.

- 정의되지 않은 필드는 `extra="forbid"`로 거부
- 점수 범위: 1~5
- 기준 중요도 범위: 1~10
- 참여자 이름 최대 길이: 100자
- 선택 이유 최대 길이: 2,000자
- 질문 최대 길이: 500자
- 선택지 개수: 2~10개
- 평가 기준 개수: 1~10개
- 선택지·평가기준 라벨 최대 길이: 200자
- 공백 라벨 및 중복 라벨 거부
- 제출된 점수 키가 방의 모든 선택지와 평가기준을 정확히 포함하는지 재검증
- `first_choice`가 실제 방의 선택지인지 검증

### 3.3 익명 제출과 이름 제출 모드

방 생성 시 제출 방식을 선택할 수 있도록 구현했다.

익명 제출:

- 로그인 없이 방을 만들고 참여할 수 있다.
- `participant_name`을 보내면 API가 요청을 거부한다.
- 방 조회 응답에 참여자 이름이 노출되지 않는다.
- 개인별 점수와 이유를 조회 API로 공개하지 않는다.

이름 제출:

- 방 생성자는 Google 로그인이 필요하다.
- 참여자는 이름을 제출해야 한다.
- 동일 방에서 같은 이름으로 중복 제출하는 것을 차단한다.
- 방의 제출 현황에는 제출한 이름만 표시한다.

현재 이름은 사용자가 입력하는 표시용 값이며 Google 계정의 실제 이름과 자동 연결하거나 검증하지 않는다. 이 부분은 의도된 MVP 경계이자 후속 개선 과제다.

### 3.4 Google 로그인과 서명 세션

Google 단일 로그인과 FastAPI 세션 경계를 구현했다.

로그인 흐름:

1. 브라우저가 Google Identity Services에서 ID 토큰을 받는다.
2. 프론트엔드가 ID 토큰을 `/api/auth/google`로 전송한다.
3. 백엔드가 `google-auth`로 토큰 서명과 Client ID audience를 검증한다.
4. `sub`, 이메일, `email_verified`, 이름과 프로필 이미지를 확인한다.
5. 검증이 끝난 사용자 정보로 자체 세션 토큰을 만든다.
6. 브라우저에 HttpOnly 쿠키로 세션을 전달한다.

세션 보호 방식:

- HMAC-SHA256으로 세션 payload에 서명
- 최소 32자 이상의 `SESSION_SECRET` 요구
- 운영 환경에서 `Secure=true` 적용
- JavaScript 접근을 막기 위한 `HttpOnly`
- 기본적인 교차 사이트 요청 제한을 위한 `SameSite=Lax`
- 기본 만료 시간 7일
- 설정 가능한 TTL을 최소 5분, 최대 30일로 제한
- `hmac.compare_digest`로 서명 비교
- 변조되거나 만료된 토큰은 인증 실패 처리

현재 세션은 암호화 토큰이 아니라 서명 토큰이다. 즉, payload의 변조는 막지만 내용을 암호화하는 구조는 아니다.

### 3.5 로그인 정보와 의견 데이터 분리

Google 계정의 `sub`와 이메일을 의견 Submission에 자동으로 저장하지 않도록 했다.

- 인증 정보는 세션 사용자 확인에 사용한다.
- 익명 의견에는 참여자 이름, Google `sub`, 이메일을 연결하지 않는다.
- 이름 제출도 현재는 사용자가 입력한 표시 이름만 저장한다.
- 통계 결과에는 개인 계정 정보가 포함되지 않는다.

이 구조는 로그인 기능을 제공하면서도 의견 데이터가 자동으로 개인 계정에 귀속되는 것을 피하기 위한 설계다.

### 3.6 저장소 추상화와 DynamoDB 연동

같은 API 코드를 로컬과 운영 환경에서 사용할 수 있도록 `RoomStore`를 구성했다.

로컬 환경:

- 별도 환경변수가 없으면 인메모리 dict 사용
- 빠른 개발과 테스트에 적합
- 프로세스가 종료되면 데이터가 사라짐

운영 환경:

- `CONSENSUS_TABLE_NAME`이 설정되면 DynamoDB 사용
- 방 전체를 Pydantic JSON으로 직렬화해 저장
- 방 생성 시 `attribute_not_exists(code)` 조건으로 코드 충돌 방지
- 조회 시 `ConsistentRead=True` 사용
- App Runner 컨테이너가 교체되어도 방 데이터 유지

### 3.7 통계 엔진 연결

모든 정량 결과는 LLM이 아니라 NumPy 통계 엔진에서 계산한다.

- 선택지별 1순위 비율
- 팀 평균 중요도
- 중요도 합의 수준
- 선택지·기준별 점수 합의 수준
- 가중 평균 점수와 현재 승자
- 기준 중요도를 변화시킨 1,000회 시뮬레이션 기반 안정성
- 안정성이 가장 높은 robust choice
- 특정 기준의 중요도가 얼마나 변하면 결과가 뒤집히는지 계산하는 weight Flip Point
- 한 명의 의견을 제외할 때 결과가 바뀌는 member Flip Point
- 다수 선택 내부의 낮은 합의와 반복 우려를 이용한 숨은 갈등 탐지
- 결과를 뒤집기 쉬운 쟁점을 중심으로 한 토론 안건 생성

시뮬레이션 seed를 고정해 같은 입력에는 같은 결과가 나오도록 구성했다. 이는 데모와 테스트의 재현성을 높인다.

### 3.8 선택적 LLM 연동과 장애 격리

OpenAI 연동은 핵심 계산이 아닌 자연어 보조 기능으로 제한했다.

- 자유 서술 의견을 제공된 선택지·평가기준 범주로 구조화
- 통계 결과를 기반으로 Devil's Advocate 질문 생성
- Defender 답변을 `resolved`, `open`, `reframed`로 판정

안전장치:

- `OPENAI_API_KEY`가 없어도 핵심 기능 정상 동작
- 네트워크 오류, timeout, 잘못된 JSON, 스키마 오류를 실패 값으로 변환
- API 경계에서 예상하지 못한 LLM 예외도 격리
- JSON Schema와 Pydantic으로 응답 재검증
- 제공되지 않은 선택지·평가기준을 반환하면 폐기
- 질문 개수, 중복, 한국어 포함 여부와 숫자 주장 여부 검증
- LLM 실패 시 결정적 fallback 질문과 판정 사용
- LLM이 수치, 순위 또는 최종 결정을 생성하지 못하도록 역할 제한

### 3.9 Devil's Advocate 공방 통합

통계 결과 이후 팀이 반론을 검토할 수 있는 백엔드 흐름을 통합했다.

- 분석 시점의 증거 snapshot 고정
- Challenger 질문 2~3개 생성
- 질문과 증거 키 연결
- Defender가 근거, 미확인 사항, 완화책 제출
- 최종 Challenger 판정
- `resolved`, `open`, `reframed` 상태 제공
- 해결되지 않은 쟁점을 분석 화면의 토론 안건으로 다시 반영
- 한 번 생성한 분석·질문을 저장하고 반복 호출 시 재사용

공격자 프롬프트 명세 자체는 협업 담당 영역이며, Build-B에서는 해당 명세가 FastAPI 분석 및 공방 흐름에서 안전하게 동작하도록 통합 경계를 담당했다.

### 3.10 동시성 제어

단일 프로세스에서 같은 방에 동시에 요청이 들어오는 경우를 처리하기 위해 방별 Lock을 적용했다.

- 마지막 제출이 동시에 여러 번 들어와도 예정 인원을 초과하지 않도록 재검사
- 같은 참여자 이름의 중복 제출 방지
- Devil's Advocate가 반복·동시 요청에서 한 번만 생성되도록 제어
- 종료된 공방에 중복 답변이 들어오는 것을 차단

현재 Lock은 프로세스 내부에서만 유효하다. 운영은 App Runner 인스턴스를 1개로 고정해 이 경계에 맞췄다.

### 3.11 Docker 및 배포 통합

프론트엔드와 백엔드를 하나의 Docker 이미지로 배포할 수 있도록 구성했다.

- Node 기반 React 빌드 단계
- Python 3.12 기반 FastAPI 런타임 단계
- FastAPI가 React `dist` 정적 파일까지 서빙
- 컨테이너 포트 8080
- `/api/health` health check
- ECR 이미지 저장
- AWS App Runner 배포
- Git SHA 기반 불변 롤백 태그
- GitHub Actions 자동 배포 흐름
- 운영 환경변수로 Google, 세션, DynamoDB, OpenAI 설정 분리

### 3.12 자동화 테스트와 종합 검증

현재 저장소 기준 전체 백엔드 테스트 85개가 통과한다.

검증 범위:

- 방 생성·조회·제출·분석 정상 흐름
- 빈 값, 잘못된 범위, 누락·추가 키, 긴 입력
- 익명 방 이름 차단
- 이름 제출 방 인증 및 이름 중복 차단
- 방 정원 초과와 동시 최종 제출
- 전원 제출 전 분석 차단
- Google 설정 누락, 잘못된 토큰, 미인증 이메일
- Google 검증 서비스 장애
- 세션 서명 변조, 만료, TTL 범위, 로그아웃
- 인메모리 및 가짜 DynamoDB 저장 계약
- LLM 키 누락, timeout, 잘못된 JSON과 예상 밖 예외
- Devil's Advocate 생성 캐시와 fallback
- 통계 계산과 데모 결과 계약

릴리스 게이트에서 추가로 확인하는 항목:

1. 전체 백엔드 테스트
2. 프론트엔드 production build
3. 선택적 실제 OpenAI smoke test
4. Docker 이미지 빌드
5. 실행 컨테이너 health check
6. 프론트가 Mock이 아닌 LIVE API 번들인지 검사
7. HTTP로 실제 데모 데이터를 제출하고 분석 결과 계약 검사

### 3.13 테스트 과정에서 발견하고 수정한 문제

#### 선택적 LLM 오류가 제출 전체를 중단하던 문제

- 문제: LLM 어댑터 내부의 예상하지 못한 예외가 API 밖으로 전파돼 제출이 5xx로 실패할 수 있었다.
- 수정: 의견 구조화 호출을 API 경계에서 예외 격리하고, 실패 시 `parsed=None`으로 제출을 계속한다.
- 결과: 부가 기능의 장애가 핵심 숫자 제출 경로에 영향을 주지 않는다.

#### 옵션·평가기준 라벨 길이가 무제한이던 문제

- 문제: 매우 긴 라벨이 중첩된 제출 키와 API 계약을 불필요하게 크게 만들 수 있었다.
- 수정: 옵션과 평가기준 라벨을 각각 최대 200자로 제한했다.
- 결과: 모델과 API 입력 크기의 경계가 명확해졌다.

---

## 4. 현재 API 요약

### 인증

- `GET /api/auth/config`: 로그인 활성화 여부와 공개 Google Client ID
- `POST /api/auth/google`: Google ID 토큰 검증 및 세션 쿠키 발급
- `GET /api/auth/me`: 현재 로그인 상태 조회
- `POST /api/auth/logout`: 세션 쿠키 삭제

### 방과 제출

- `POST /api/rooms`: 방 생성
- `GET /api/rooms/{code}`: 방 정보, 제출 인원과 완료 상태 조회
- `POST /api/rooms/{code}/submit`: 의견 제출
- `GET /api/rooms/{code}/analysis`: 통계 분석과 Challenger 질문 조회

### 공방

- `GET /api/rooms/{code}/debate`: 증거 snapshot과 공방 기록 조회
- `POST /api/rooms/{code}/debate/defend`: Defender 답변과 최종 판정 처리

### 운영

- `GET /api/health`: 애플리케이션 프로세스 상태 확인

---

## 5. 현재 문제점과 개선하면 좋은 부분

### 우선순위 P0 — 실서비스 전 반드시 개선

#### 다중 인스턴스 제출 경쟁

현재 방별 Lock은 한 FastAPI 프로세스 안에서만 동작한다. App Runner 인스턴스가 두 개 이상이면 각 인스턴스가 동시에 같은 room JSON을 읽고 덮어쓸 수 있다.

개선안:

- Submission을 방 JSON 내부 배열이 아니라 별도 DynamoDB 아이템으로 저장
- `room_code + submission_id` 또는 `room_code + participant_token`을 키로 사용
- DynamoDB ConditionExpression으로 정원과 중복 제출을 원자적으로 처리
- Room에 version을 두고 optimistic locking 적용
- 다중 인스턴스 부하·경쟁 테스트 추가

#### 방 접근 권한과 참여자 인증

현재 6자리 방 코드를 아는 사용자는 방을 조회하고 제출할 수 있다. 이름 제출의 이름도 Google 계정과 실제로 연결되지는 않는다.

개선안:

- 방 생성자 권한과 참여자 권한 분리
- 초대 링크에 만료 가능한 일회성 participant token 사용
- 이름 제출 참여자는 Google 로그인 또는 조직 이메일로 검증하는 선택 옵션 제공
- 방 종료, 재개, 참여자 제거 권한을 생성자에게 제공
- 제출 완료 후 재제출·수정 정책을 명시

#### 요청 남용과 방 코드 대입 공격

현재 애플리케이션 수준 rate limit이 없다. 6자리 코드는 편리한 공유 수단이지만 강한 인증 수단은 아니다.

개선안:

- IP·세션·방 코드 기준 rate limit
- 연속 실패 시 지수 backoff 또는 일시 차단
- AWS WAF 또는 API 레벨 limiter 도입
- 방 조회와 제출 시도에 대한 보안 이벤트 로그
- 의심스러운 트래픽 알림

#### 개인정보 처리 및 데이터 수명

방에는 의견 이유, 점수와 이름이 저장되며, OpenAI 키가 설정되면 이유 텍스트가 자연어 구조화를 위해 외부 API로 전달될 수 있다.

개선안:

- OpenAI 처리 여부를 UI에서 명시하고 동의 받기
- 개인정보 처리방침과 보존 기간 정의
- DynamoDB TTL로 방 자동 만료
- 방 생성자용 즉시 삭제 기능
- 로그에 원문 의견과 토큰이 남지 않도록 구조화 로깅 규칙 적용
- 필요하면 민감한 이유 텍스트를 별도 암호화 저장

#### CSRF 및 웹 보안 강화

현재 HttpOnly, Secure, SameSite=Lax와 CORS allowlist를 사용하지만 별도 CSRF 토큰은 없다.

개선안:

- 상태 변경 요청에 CSRF 토큰 또는 엄격한 Origin 검증 적용
- Content Security Policy 적용
- HSTS와 주요 보안 응답 헤더 적용
- 허용 HTTP method와 header를 실제 필요 범위로 축소
- 보안 헤더 자동화 테스트 추가

### 우선순위 P1 — 안정성과 운영 품질 개선

#### 세션 즉시 폐기 기능

현재 세션은 stateless 서명 토큰이어서 로그아웃은 브라우저 쿠키 삭제로 처리한다. 탈취된 토큰을 서버에서 즉시 폐기할 수는 없다.

개선안:

- Redis 또는 DynamoDB 기반 opaque session ID 사용
- 사용자별 `session_version`을 이용한 전체 로그아웃
- 짧은 세션 TTL과 재인증 정책
- 의심 로그인 감지와 세션 목록 관리

#### 시크릿 관리

운영 환경변수는 코드와 이미지에서 분리되어 있지만, 민감한 값은 전문 시크릿 저장소를 사용하는 편이 안전하다.

개선안:

- `SESSION_SECRET`, `OPENAI_API_KEY`를 AWS Secrets Manager 또는 SSM Parameter Store로 이전
- 정기 회전 절차와 담당자 정의
- 키 노출 시 폐기·재발급 runbook 작성
- 운영 로그와 CI 출력에서 값 마스킹 확인

#### 관측성과 장애 대응

현재 health endpoint는 FastAPI 프로세스가 동작하는지만 확인한다. DynamoDB나 외부 서비스 상태는 포함하지 않는다.

개선안:

- liveness와 readiness endpoint 분리
- DynamoDB 접근 가능 여부를 readiness에서 제한적으로 확인
- 요청 ID, room code hash, latency, status code를 구조화 로그로 기록
- 제출·분석·LLM 호출 성공률과 지연시간 metric 수집
- 4xx 급증, 5xx, OpenAI fallback 비율에 alert 설정
- 개인정보를 제외한 감사 로그 설계

#### 저장 스키마와 마이그레이션

현재 방 전체를 하나의 JSON으로 저장해 구현은 단순하지만, 데이터가 커지거나 스키마가 바뀌면 부분 갱신과 마이그레이션이 어렵다.

개선안:

- room schema version 필드 추가
- Room, Submission, Analysis, Debate 아이템 분리
- 마이그레이션 및 하위 호환 정책 정의
- DynamoDB item 크기 제한을 고려한 제출 수·텍스트 길이 설계
- 오래된 스키마 fixture로 호환 테스트 추가

#### 멱등성

네트워크 재시도 때문에 같은 제출 요청이 두 번 전송될 수 있다. 이름 제출은 중복 이름으로 일부 차단하지만 익명 제출에는 안정적인 중복 식별자가 없다.

개선안:

- 제출 요청에 `Idempotency-Key` 도입
- 참여자별 일회성 제출 토큰 발급
- 같은 키의 응답을 일정 시간 재사용
- 모바일 네트워크 재시도 E2E 테스트 추가

#### 테스트 의존성 경고

현재 전체 테스트는 통과하지만 FastAPI TestClient 관련 deprecation warning이 있다.

개선안:

- FastAPI, Starlette, HTTPX 권장 조합 확인
- 의존성 버전을 갱신하고 warning을 0개로 유지
- CI에서 알려진 warning만 명시적으로 관리

### 우선순위 P2 — 제품 경험 및 확장 아이디어

#### 가명 제출 모드 구체화

익명과 이름 제출 사이에 팀이 서로를 구분하되 실제 신원은 숨기는 가명 모드를 추가할 수 있다.

아이디어:

- 서버가 `참여자 A`, `푸른 여우` 같은 방별 가명을 자동 생성
- 같은 참여자는 방 안에서 동일한 가명을 유지
- 방이 달라지면 다른 가명을 발급해 교차 추적 방지
- 생성자도 실제 계정과 가명의 연결을 볼 수 없게 선택 가능

#### 사용자 프로필 및 조직 연동

- Google `sub`를 기준으로 내부 사용자 프로필 생성
- 닉네임과 프로필 이미지 관리
- 조직 도메인 제한 방
- 자주 사용하는 평가 기준 템플릿 저장
- 내가 만든 방과 참여한 방 기록

계정과 의견의 연결은 기본값이 아니라 사용자가 이해할 수 있는 명시적 정책으로 제공해야 한다.

#### 실시간 진행 상태

현재 polling은 단순하고 데모 안정성이 높지만 요청이 불필요하게 반복된다.

개선안:

- 제출 인원과 방 종료 상태에 SSE 적용
- 양방향 협업 기능이 필요할 때만 WebSocket 도입
- 연결 실패 시 polling fallback 유지

#### 분석 결과 신뢰도 표현

소수 인원에서는 안정성 수치가 과도하게 정밀해 보일 수 있다.

개선안:

- 표본 수가 적을 때 주의 문구 제공
- 수치를 소수점 대신 구간 또는 등급으로 표현하는 옵션
- 동점 규칙과 시뮬레이션 가정을 UI에서 설명
- 왜 해당 토론 안건이 생성됐는지 근거 펼쳐보기 제공
- LLM 결과에 `live` 또는 `fallback` 출처 표시

#### 결정 이후 회고

싱큐의 가치는 결정 시점뿐 아니라 결정 이후에도 확인할 수 있다.

아이디어:

- 결정 당시 가정과 열린 쟁점 저장
- 일정 기간 뒤 실제 결과와 비교하는 회고 알림
- 어떤 Flip Point가 실제 위험으로 이어졌는지 기록
- 팀별 반복 갈등 기준을 개인정보를 침해하지 않는 집계 형태로 제공

#### 방 템플릿과 재사용

- 채용, 제품 우선순위, 기술 선택, 행사 아이디어 등 상황별 템플릿
- 평가 기준 추천은 하되 사용자가 최종 확정
- 과거 방을 복제하되 기존 의견은 복사하지 않음
- 결과를 PDF 또는 회의록 형식으로 내보내기

---

## 6. 권장 후속 로드맵

### 1단계 — 데모 이후 즉시

- 운영 Google 로그인 E2E 재검증
- 실제 DynamoDB 방 생성·제출·재시작 복구 검증
- App Runner 환경변수와 Secure cookie 확인
- DynamoDB TTL과 방 삭제 정책 결정
- 실제 OpenAI 사용 시 개인정보 안내 추가
- 테스트 수와 문서 수치 `85 passed`로 통일

### 2단계 — 베타 서비스 전

- Submission 별도 아이템 저장 및 원자적 동시성 처리
- 초대용 participant token과 방 권한 모델 도입
- rate limit, CSRF/Origin 검증, 보안 헤더 추가
- Secrets Manager 연동
- liveness/readiness 분리 및 CloudWatch 지표·알림 설정
- 멱등성 키와 재시도 정책 구현
- 데이터 보존·삭제 정책 구현

### 3단계 — 사용자 검증 단계

- 방별 자동 가명 기능 실험
- 결과 설명과 표본 수 경고 개선
- SSE 기반 실시간 제출 현황
- 결정 이후 회고 기능
- 방 템플릿과 결과 내보내기
- 사용자 인터뷰로 익명·가명·이름 제출의 실제 사용 맥락 검증

### 4단계 — 확장 단계

- 다중 App Runner 인스턴스 부하 테스트
- 조직·워크스페이스·역할 기반 권한
- 감사 로그와 관리자 정책
- 데이터 스키마 마이그레이션 체계
- SLO, 장애 대응 및 복구 훈련

---

## 7. 후속 검증 체크리스트

### 기능

- 익명 방에서 이름과 계정 정보가 저장·노출되지 않는가?
- 이름 또는 가명 방의 신원 정책이 사용자에게 명확한가?
- 재시도와 동시 제출에도 정원이 초과되지 않는가?
- 전원 제출 전에 결과가 노출되지 않는가?
- LLM이 실패해도 제출과 통계 분석이 완료되는가?

### 보안

- Google audience와 `email_verified`를 검증하는가?
- 세션 변조와 만료를 거부하는가?
- 운영 쿠키에 Secure, HttpOnly, SameSite가 적용됐는가?
- 시크릿이 Git, Docker layer, CI 로그에 포함되지 않는가?
- 방 코드 대입과 요청 남용을 제한하는가?
- CSRF, XSS, 보안 헤더 테스트가 있는가?

### 개인정보

- 어떤 데이터가 OpenAI로 전달되는지 사용자가 알 수 있는가?
- 원문 의견과 계정 정보가 로그에 남지 않는가?
- 사용자가 방과 의견을 삭제할 수 있는가?
- 데이터 보존 기간이 정해져 있는가?
- 가명이 여러 방에서 사용자를 추적하는 식별자가 되지 않는가?

### 운영

- 실제 DynamoDB와 IAM을 사용한 E2E가 통과하는가?
- 컨테이너 교체 후 기존 방이 복구되는가?
- readiness가 저장소 장애를 감지하는가?
- 오류율, latency, fallback 비율을 확인할 수 있는가?
- 직전 Git SHA 이미지로 롤백할 수 있는가?

---

## 8. 발표용 요약

> Build-B에서는 FastAPI 기반으로 방 생성, 의견 제출, 분석과 공방까지 전체 백엔드 흐름을 통합했습니다. Google ID 토큰을 서버에서 검증하고 HMAC 서명 HttpOnly 세션을 발급했으며, 로그인 계정과 의견 데이터는 분리했습니다. 로컬 인메모리 저장소와 운영 DynamoDB를 같은 인터페이스로 연결하고, LLM은 자연어 보조 기능으로 격리해 장애가 나더라도 NumPy 기반 핵심 분석은 계속 동작하도록 했습니다. 현재 85개의 자동화 테스트와 Docker 릴리스 게이트를 통과하며, 실서비스 확장 시에는 DynamoDB 원자적 동시성, 방 접근 권한, rate limit, CSRF와 개인정보 수명 관리를 우선 개선할 계획입니다.

---

## 9. 관련 문서와 코드

- `README.md`: 제품 및 실행 방법
- `SPEC.md`: API와 데이터 계약
- `PLAN.md`: 역할과 통합 계획
- `BACKEND_VALIDATION_REPORT.md`: 백엔드 종합 검증 결과
- `DEPLOYMENT_PLAN.md`: AWS 배포와 운영 계획
- `backend/main.py`: FastAPI 라우터와 통합 로직
- `backend/auth.py`: Google 인증 및 서명 세션
- `backend/models.py`: Pydantic 데이터 모델
- `backend/storage.py`: 인메모리·DynamoDB 저장소
- `backend/stats.py`: 결정적 통계 엔진
- `backend/llm.py`: 선택적 OpenAI 연동
- `backend/debate.py`: 증거 기반 공방 상태 관리
- `backend/tests/`: 백엔드 자동화 테스트
- `scripts/release_gate.sh`: 통합 릴리스 게이트
